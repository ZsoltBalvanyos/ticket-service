server:
  port : 9992

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/core
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  sql:
    init:
      mode: always